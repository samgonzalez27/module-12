# Example environment file - copy to .env and fill secrets before running

# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=calculator
# Full connection string used by the app. Update if you change the db settings above.
DATABASE_URL=postgresql://postgres:changeme@db:5432/calculator

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=changeme

# App
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000
